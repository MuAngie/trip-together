# Design System

## Overview

移动优先的旅行工作页面。整体沿用 Travel-Plan-Page 的纸张感编辑风格，在米白底色上使用深墨色、湖蓝、森林绿和陶土橙。视觉层级依赖留白、字号和细线分隔，不依赖大量卡片。

## Color Palette

- Paper: `oklch(96.5% 0.012 150)`，页面背景
- Surface: `oklch(99% 0.006 150)`，表单和重要内容表面
- Ink: `oklch(26% 0.035 215)`，正文和深色信息块
- Muted: `oklch(50% 0.025 200)`，次要信息
- Lake: `oklch(52% 0.09 210)`，链接、选择和地图操作
- Forest: `oklch(47% 0.07 145)`，完成状态
- Terracotta: `oklch(57% 0.13 45)`，提醒和重点状态
- Line: `oklch(87% 0.018 175)`，分隔线

## Typography

使用系统无衬线字体。正文 17px，行高 1.65；辅助信息不小于 13px；二级标题 30px；时间和机场代码可使用系统等宽字体。

## Layout

内容最大宽度 720px，手机端左右留白 18px。顶部导航固定，页面使用纵向章节组织。章节间距 40 至 48px，列表项以细线分隔。仅航班、下一事项、路线图和记账输入等独立工作区使用完整表面。

## Components

- 顶部导航：44px 以上触控区，旅行信息和记账入口始终可见。
- 下一事项：深墨色主表面，突出日期、动作和状态。
- 日程时间线：日期为主标题，地点和时间清楚分列，可展开查看。
- 路线图：冻结底图加确定性路线叠加，支持查看每天路线和地点跳转。
- 清单：大复选框，完成后保留可读文字并显示状态。
- 记账：多人、多币种、付款人和参与人选择，金额使用等宽数字。

## Motion

仅对展开、选择和保存反馈使用 150 至 220ms 状态过渡。系统要求减少动态效果时关闭平滑滚动和非必要过渡。

## Responsive Behavior

320px 起可用。窄屏保持单列，横向航班卡允许滑动；长订单号和地点名称允许换行。底部安全区和顶部安全区使用环境变量处理。
